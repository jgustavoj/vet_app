# This file defines what outside code can import from the views sub-package.
# In this case, from . import * would work, but as a general rule defining these
# explicitly is preferable. 
from . import setup, public, home, clients
